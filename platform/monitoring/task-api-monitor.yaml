apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: task-api
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: task-api
  endpoints:
  - port: http
    path: /metrics

